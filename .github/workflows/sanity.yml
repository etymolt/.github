name: sanity
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "org profile - no tests"
